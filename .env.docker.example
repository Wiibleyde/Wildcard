############################################################
# Wildcard — Docker Compose (Supabase + app Next.js)
# Usage : docker compose --env-file .env.docker up -d
# Copier dans .env.docker et remplir les valeurs. NE PAS COMMITER.
############################################################

# ── Compose — override PROD ──────────────────────────────
# Décommenter SUR LE SERVEUR DE PROD pour que toute commande docker compose
# (dont scripts/deploy.sh) charge l'override loopback + réseau `edge` sans -f.
# NE PAS activer en dev local. Prérequis serveur : `docker network create edge`.
# Le reverse proxy + l'observabilité (Caddy, CrowdSec, Prometheus, Grafana,
# Umami) sont déployés séparément — cf. deploy/README.md.
# COMPOSE_FILE=docker-compose.yml:docker-compose.prod.yml

# ── Postgres ──────────────────────────────────────────────
POSTGRES_HOST=db
POSTGRES_DB=postgres
POSTGRES_PORT=5432
POSTGRES_PASSWORD=your-super-secret-db-password

# ── JWT ───────────────────────────────────────────────────
# Generate with: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-token-with-at-least-32-characters
JWT_EXPIRY=3600
SECRET_KEY_BASE=your-secret-key-base-for-realtime

# ── API Keys (generated from JWT_SECRET) ─────────────────
# Use: https://supabase.com/docs/guides/self-hosting/docker#generate-api-keys
ANON_KEY=<generated-anon-jwt>
SERVICE_ROLE_KEY=<generated-service-role-jwt>

# ── URLs ──────────────────────────────────────────────────
SITE_URL=http://localhost:3000
API_EXTERNAL_URL=http://localhost:54321
SUPABASE_PUBLIC_URL=http://localhost:54321
ADDITIONAL_REDIRECT_URLS=http://localhost:3000/auth/callback

# ── Reverse proxy & TLS (stack edge séparée) ─────────────
# Le point d'entrée HTTPS (Caddy) et l'IPS (CrowdSec) ne vivent PLUS dans ce
# dépôt : ils tournent dans la stack edge mutualisée (cf. deploy/README.md).
# Domaines, ACME_EMAIL et CROWDSEC_API_KEY se configurent LÀ-BAS. Ici, aligner
# simplement les URLs publiques ci-dessus (SITE_URL / API_EXTERNAL_URL /
# SUPABASE_PUBLIC_URL) sur les domaines servis par ce Caddy central.

# ── App image (CD) ───────────────────────────────────────
# Laisser vide en local (l'image est buildée par `up --build`).
# En prod/CD : pointer sur l'image publiée par la pipeline GHCR.
#   APP_IMAGE=ghcr.io/wiibleyde/wildcard:latest
APP_IMAGE=

# ── Kong ports ────────────────────────────────────────────
KONG_HTTP_PORT=54321
KONG_HTTPS_PORT=54322

# ── Studio ────────────────────────────────────────────────
STUDIO_PORT=54323
STUDIO_DEFAULT_ORGANIZATION=Wildcard
STUDIO_DEFAULT_PROJECT=Wildcard Dev
DASHBOARD_USERNAME=admin
DASHBOARD_PASSWORD=admin

# ── Inbucket (local email) ────────────────────────────────
INBUCKET_PORT=54324

# ── PostgREST ─────────────────────────────────────────────
PGRST_DB_SCHEMAS=public,storage,graphql_public

# ── Auth signup flags ─────────────────────────────────────
DISABLE_SIGNUP=false
ENABLE_EMAIL_SIGNUP=true
ENABLE_EMAIL_AUTOCONFIRM=true
ENABLE_ANONYMOUS_USERS=false

# ── SMTP (local dev → use inbucket) ──────────────────────
SMTP_ADMIN_EMAIL=admin@example.com
SMTP_HOST=supabase-inbucket
SMTP_PORT=2500
SMTP_USER=fake_mail_user
SMTP_PASS=fake_mail_password
SMTP_SENDER_NAME=Wildcard

# ── Auth email paths ──────────────────────────────────────
MAILER_URLPATHS_INVITE=/auth/v1/verify
MAILER_URLPATHS_CONFIRMATION=/auth/v1/verify
MAILER_URLPATHS_RECOVERY=/auth/v1/verify
MAILER_URLPATHS_EMAIL_CHANGE=/auth/v1/verify

# ── OAuth providers ───────────────────────────────────────
# Register redirect URL in each provider console:
#   http://localhost:54321/auth/v1/callback

ENABLE_DISCORD_SIGNUP=false
SUPABASE_AUTH_DISCORD_CLIENT_ID=
SUPABASE_AUTH_DISCORD_SECRET=

ENABLE_GOOGLE_SIGNUP=false
SUPABASE_AUTH_GOOGLE_CLIENT_ID=
SUPABASE_AUTH_GOOGLE_SECRET=

# ── Analytics ─────────────────────────────────────────────
# Generate with: openssl rand -base64 32
LOGFLARE_API_KEY=your-logflare-api-key

# ── Observabilité & analytics (stack edge séparée) ───────
# Prometheus / Grafana / Umami tournent dans la stack edge mutualisée
# (cf. deploy/README.md). L'app conserve ces deux points d'intégration :

# Bearer token protégeant /api/metrics. Le Prometheus central scrape l'endpoint
# et DOIT présenter la même valeur. Générer : openssl rand -hex 32. Laissé vide
# en dev local (pas de Prometheus) → la route reste ouverte.
METRICS_TOKEN=change-me-metrics-token

# Tag Umami injecté dans l'app au RUNTIME (window.__PUBLIC_ENV__). UMAMI_URL =
# l'Umami central, joignable depuis le NAVIGATEUR. WEBSITE_ID = l'ID du site créé
# dans l'UI Umami ; vide tant qu'il n'existe pas → le tag ne se charge pas.
UMAMI_URL=http://localhost:54325
UMAMI_WEBSITE_ID=
